<Window x:Class="WarehouseManagement.ChangePasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WarehouseManagement"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:uc="clr-namespace:WarehouseManagement.UserControlG"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        WindowStyle="None"
        Title="Đổi mật khẩu"
        Height="450"
        Width="400"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        
        <Grid Grid.Row="0">
            <uc:ControlBarUC></uc:ControlBarUC>
        </Grid>

        <Grid Grid.Row="1">
            <materialDesign:Card Margin="15" Padding="15">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="ĐỔI MẬT KHẨU"
                               HorizontalAlignment="Center"
                               FontWeight="Bold"
                               FontSize="22"
                               Foreground="{DynamicResource PrimaryHueMidBrush}"
                               Margin="0,0,0,20" />

                    <!-- Ô Username (Chỉ đọc) -->
                    <TextBox x:Name="txtUsername"
                             Height="40"
                             materialDesign:HintAssist.Hint="Tài khoản"
                             FontSize="16"
                             Margin="0,0,0,15"
                             IsReadOnly="True"
                             Style="{StaticResource MaterialDesignFloatingHintTextBox}" />

                    <!-- Ô Mật khẩu cũ -->
                    <PasswordBox x:Name="txtOldPassword"
                                 Height="40"
                                 materialDesign:HintAssist.Hint="Mật khẩu cũ"
                                 FontSize="16"
                                 Margin="0,0,0,15"
                                 Style="{StaticResource MaterialDesignFloatingHintPasswordBox}" />

                    <!-- Ô Mật khẩu mới -->
                    <PasswordBox x:Name="txtNewPassword"
                                 Height="40"
                                 materialDesign:HintAssist.Hint="Mật khẩu mới"
                                 FontSize="16"
                                 Margin="0,0,0,15"
                                 Style="{StaticResource MaterialDesignFloatingHintPasswordBox}" />

                    <!-- Ô Xác nhận mật khẩu mới -->
                    <PasswordBox x:Name="txtConfirmPassword"
                                 Height="40"
                                 materialDesign:HintAssist.Hint="Xác nhận mật khẩu mới"
                                 FontSize="16"
                                 Margin="0,0,0,25"
                                 Style="{StaticResource MaterialDesignFloatingHintPasswordBox}" />

                    <!-- Nhóm Nút bấm -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Button x:Name="btnConfirm"
                                Content="Xác nhận"
                                Width="120"
                                Height="40"
                                Margin="0,0,15,0"
                                Style="{StaticResource MaterialDesignRaisedButton}"
                                Click="btnConfirm_Click" />
                        <Button x:Name="btnCancel"
                                Content="Hủy"
                                Width="120"
                                Height="40"
                                Style="{StaticResource MaterialDesignOutlinedButton}"
                                Click="btnCancel_Click" />
                    </StackPanel>
                </StackPanel>
            </materialDesign:Card>
        </Grid>
    </Grid>
</Window>
